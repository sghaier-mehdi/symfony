{# templates/admin_reponse/_form.html.twig #}
{{ form_start(form) }} {# Assuming the form variable is named 'form' #}
    {{ form_row(form.contenu) }} {# Assuming your ReponseType has a 'contenu' field #}
    {# Add other fields from your ReponseType if any #}

    <button type="submit" class="btn btn-primary mt-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}