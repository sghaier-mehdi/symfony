{# templates/category/new.html.twig #}
{% extends 'back/base.html.twig' %}

{% block title %}Create New Category{% endblock %}

{% block body %}
    <div class="admin-form-container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Create New Category</h1>
            <a href="{{ path('app_category_index') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left-circle"></i> Back to List
            </a>
        </div>

        {# Include the form partial #}
        {{ include('category/_form.html.twig', {'button_label': 'Create Category'}) }}
    </div>
{% endblock %}  