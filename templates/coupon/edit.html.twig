{% extends 'back/base.html.twig' %}

{% block title %}Edit Coupon{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h1 class="h3 mb-0">Edit Coupon</h1>
                        <div>
                            <a href="{{ path('app_coupon_index') }}" class="btn btn-secondary btn-sm">
                                <i class="fa fa-arrow-left"></i> Back to list
                            </a>
                        </div>
                    </div>
                    <div class="card-body">
                        {{ include('coupon/_form.html.twig', {'button_label': 'Update'}) }}

                        <div class="mt-3">
                            {{ include('coupon/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %} 