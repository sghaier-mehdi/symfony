<form method="post" action="{{ path('app_objective_delete', {'id': objective.id}) }}" onsubmit="return confirm('Are you sure you want to delete this objective?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objective.id) }}">
    <button class="btn btn-danger">
        <i class="bi bi-trash"></i> Delete
    </button>
</form> 