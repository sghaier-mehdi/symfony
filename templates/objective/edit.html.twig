{% extends 'back/base.html.twig' %}

{% block title %}Edit Objective{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h1 class="h3 mb-0">Edit Objective</h1>
                        <div>
                            <a href="{{ path('app_objective_index') }}" class="btn btn-secondary btn-sm">
                                <i class="bi bi-arrow-left"></i> Back to list
                            </a>
                        </div>
                    </div>
                    <div class="card-body">
                        {{ include('objective/_form.html.twig', {'button_label': 'Update'}) }}

                        <div class="mt-3">
                            {{ include('objective/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %} 